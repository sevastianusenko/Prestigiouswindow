// Shared by /privacy and /terms so the two pages never drift apart.
// Bump this by hand whenever the substance of either page changes — it is the
// date customers rely on, so it must not move on every unrelated deploy.
export const LEGAL_EFFECTIVE_DATE = "September 19, 2026";
